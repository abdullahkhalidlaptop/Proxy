services:
  - type: web
    name: yt-dl-proxy
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn proxy:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
